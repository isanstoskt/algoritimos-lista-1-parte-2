#include <stdio.h>

int main() {
    int vec[10], contagem = 0;
    for (int i = 0; i < 10; i++) {
        scanf("%d", &vec[i]);
        if (vec[i] % 2 == 0) {
            contagem++;
        }
    }
    printf("valores pares: %d\n", contagem);
    return 0;
}
