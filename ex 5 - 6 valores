#include <stdio.h>

int main() {
    int vec[6];
    for (int i = 0; i < 6; i++) {
        scanf("%d", &vec[i]);
    }
    printf("vetor em ordem inversa:");
    for (int i = 5; i >= 0; i--) {
        printf("%d ", vec[i]);
    }
    printf("\n");
    return 0;
}
