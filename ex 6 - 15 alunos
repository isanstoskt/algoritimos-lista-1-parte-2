#include <stdio.h>

int main() {
    float notas[15], sum = 0;
    for (int i = 0; i < 15; i++) {
        scanf("%f", &notas[i]);
        sum += notas[i];
    }
    printf("media %.2f\n", sum / 15.0);
    return 0;
}


